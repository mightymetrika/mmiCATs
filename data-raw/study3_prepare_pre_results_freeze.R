# Study 3 empirical illustration: pre-results freeze
# Thin wrapper: substantive freeze creation/verification logic lives under R/.
# The completed Phase 6B freeze is immutable; this call verifies it.

library(devtools)
load_all()

prepare_study3_empirical()
