% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_diagnostics.R
\name{cluster_data_explore}
\alias{cluster_data_explore}
\title{Explore Clustered Data Before Model Fitting}
\usage{
cluster_data_explore(formula, cluster, data)
}
\arguments{
\item{formula}{A two-sided formula of the form \code{outcome ~ predictor}. The
initial implementation supports one untransformed numeric predictor.}

\item{cluster}{A one-sided formula such as \code{~ school}, or a character string
naming the clustering variable.}

\item{data}{A data frame.}
}
\value{
A list containing \code{overall}, \code{cluster_summary}, \code{missingness},
\code{cluster_missingness}, \code{analysis_data}, and \code{plots}. \code{plots} is a named
list of \code{ggplot2} objects.
}
\description{
Produces structured pre-model summaries and plots for a clustered linear
analysis with one continuous focal predictor. The function is descriptive:
it does not select or recommend an estimator.
}
\examples{
dat <- data.frame(
  y = rnorm(60),
  x = rnorm(60),
  id = rep(1:6, each = 10)
)
out <- cluster_data_explore(y ~ x, ~ id, dat)
out$overall
out$cluster_summary

}
