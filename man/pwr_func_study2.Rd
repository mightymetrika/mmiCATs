% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pwr_func_study2.R
\name{pwr_func_study2}
\alias{pwr_func_study2}
\title{Simulation Study of Robust Cluster-Adjusted t Statistics with Random Slopes}
\usage{
pwr_func_study2(
  n_clusters = 20,
  cluster_size = 40,
  beta = 0,
  intercept = 0,
  random_intercept_sd = 1,
  random_slope_sd = 0.05,
  residual_sd = 1,
  x_sd = 1,
  contamination = c("none", "vertical"),
  contamination_prop = 0.05,
  contamination_size = 6,
  reps = 1000,
  alpha = 0.05,
  methods = c("rs", "ri", "cr2", "cats", "cats_trunc", "cats_robust", "cats_robustbase",
    "robust_ri", "robust_rs"),
  seed = NULL,
  keep_replicates = FALSE,
  replicate_seeds = NULL
)
}
\arguments{
\item{n_clusters}{Integer number of clusters.}

\item{cluster_size}{Integer number of observations in each cluster.}

\item{beta}{Numeric value of the population mean slope for \code{x}.}

\item{intercept}{Numeric fixed intercept.}

\item{random_intercept_sd}{Positive standard deviation of the random
intercept.}

\item{random_slope_sd}{Positive standard deviation of the random slope.}

\item{residual_sd}{Positive standard deviation of the residual error.}

\item{x_sd}{Positive standard deviation of the normally distributed
predictor.}

\item{contamination}{Character string specifying the contamination condition:
\code{"none"} or \code{"vertical"}.}

\item{contamination_prop}{Numeric proportion of observations contaminated
within each cluster. The number contaminated is rounded to the nearest
whole observation, with at least one observation selected when the
proportion is greater than zero.}

\item{contamination_size}{Positive size of the outcome contamination in
residual standard deviation units.}

\item{reps}{Integer number of simulation replications.}

\item{alpha}{Numeric significance level.}

\item{methods}{Character vector of methods to fit. Available values are
\code{"rs"}, \code{"ri"}, \code{"cr2"}, \code{"cats"}, \code{"cats_trunc"}, \code{"cats_robust"},
\code{"cats_robustbase"}, \code{"robust_ri"}, and \code{"robust_rs"}.}

\item{seed}{Optional non-negative integer random-number seed.}

\item{keep_replicates}{Logical; if \code{TRUE}, retain the replicate-level results.}

\item{replicate_seeds}{Optional integer vector of length \code{reps} giving the
exact random-number seed for each replication. This is intended for
deterministic sharded execution. When supplied, \code{seed} must be \code{NULL}.}
}
\value{
A list with three elements:
\describe{
\item{summary}{A data frame containing method-level simulation results.}
\item{replicates}{A data frame containing replicate-level results when
\code{keep_replicates = TRUE}; otherwise \code{NULL}.}
\item{settings}{A list containing the simulation settings and replication
seeds.}
}

Rejection rates, coverage rates, failure rates, and singularity rates are
reported as percentages. Their Monte Carlo standard errors are reported in
percentage points. The primary estimand is the superpopulation mean slope
\code{beta}. The realized mean of the sampled cluster slopes is retained at the
replication level as a diagnostic but is not used as the simulation truth.
}
\description{
Runs one condition from Study 2, which extends Study 1 by generating genuine
cluster-to-cluster slope heterogeneity. The comparison includes a correctly
specified independent random-intercept and random-slope model, the Study 1
random-intercept benchmark, robust random-intercept and independent
random-slope mixed models, ordinary least squares with CR2 inference,
ordinary cluster-adjusted t statistics (CATs), truncated CATs, and robust
CATs based on either \code{robust::lmRob()} or \code{robustbase::lmrob()}.
}
\details{
Data are generated from a random-intercept and random-slope model with zero
intercept-slope covariance. Optional symmetric vertical contamination is
applied within every cluster after the clean data are generated. All
requested methods are fit to the same data in each replication. The \code{"rs"}
and \code{"ri"} methods use Kenward-Roger inference and require the suggested
package \code{pbkrtest}. The \code{"robust_ri"} and \code{"robust_rs"} methods use robust
Satterthwaite inference and require the suggested package \code{robustlmm}.
}
\examples{
pwr_func_study2(
  n_clusters = 6,
  cluster_size = 20,
  random_slope_sd = 0.10,
  reps = 1,
  methods = c("rs", "ri"),
  seed = 123
)

}
\references{
Esarey, J., and Menger, A. (2019). Practical and effective approaches to
dealing with clustered data. \emph{Political Science Research and Methods}, 7(3),
541-559. \doi{10.1017/psrm.2017.42}

Pustejovsky, J. E., and Tipton, E. (2018). Small-sample methods for
cluster-robust variance estimation and hypothesis testing in fixed effects
models. \emph{Journal of Business and Economic Statistics}, 36(4), 672-683.
\doi{10.1080/07350015.2016.1247004}
}
