% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_diagnostics.R
\name{cluster_model_diagnostics}
\alias{cluster_model_diagnostics}
\title{Compare Clustered Linear-Model Diagnostics Across Methods}
\usage{
cluster_model_diagnostics(
  formula,
  cluster,
  data,
  methods = c("rs", "ri", "cr2", "cats", "cats_trunc", "cats_robust", "cats_robustbase",
    "robust_ri", "robust_rs"),
  alpha = 0.05,
  seed = 20261101L,
  leave_one_cluster_out = FALSE
)
}
\arguments{
\item{formula}{A two-sided formula of the form \code{outcome ~ predictor}.}

\item{cluster}{A one-sided formula such as \code{~ school}, or a character string
naming the clustering variable.}

\item{data}{A data frame.}

\item{methods}{Character vector containing any of \code{"rs"}, \code{"ri"}, \code{"cr2"},
\code{"cats"}, \code{"cats_trunc"}, \code{"cats_robust"}, \code{"cats_robustbase"},
\code{"robust_ri"}, or \code{"robust_rs"}.}

\item{alpha}{Significance level used for confidence intervals.}

\item{seed}{Non-negative integer used to make method fitting reproducible.}

\item{leave_one_cluster_out}{Logical; if \code{TRUE}, refit every requested method
after omitting each cluster and return estimate changes.}
}
\value{
A list containing \code{comparison}, \code{cluster_fits},
\code{cluster_slope_differences}, \code{observation_diagnostics}, \code{influence},
\code{analysis_data}, and \code{plots}. The function does not retain fitted model
objects.
}
\description{
Fits selected clustered-inference methods to the same complete-case data and
returns method-level inference, cluster-specific slopes, observation-level
diagnostics, optional leave-one-cluster-out influence results, and plots.
The function is descriptive and does not recommend or select an estimator.
}
\details{
The initial implementation supports one untransformed numeric predictor.
Internally, the requested variables are mapped to the \code{out}, \code{x}, and
\code{cluster} names used by the verified Study 1/Study 2 fitting helpers so that
the empirical diagnostics use the same fitting and extraction rules as the
simulation studies.
}
\examples{
dat <- data.frame(
  y = rnorm(100),
  x = rnorm(100),
  id = rep(1:10, each = 10)
)
out <- cluster_model_diagnostics(
  y ~ x,
  ~ id,
  dat,
  methods = c("cr2", "cats"),
  seed = 123
)
out$comparison

}
